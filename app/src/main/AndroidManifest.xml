<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.search.hinaikhan.newyorktimessearch">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/toolbar_title"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="com.search.hinaikhan.newyorktimessearch.util.SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/splashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.search.hinaikhan.newyorktimessearch.mvp.SearchActivity"
            android:label="@string/title_activity_movie_detail" >
        </activity>


        <activity
            android:name="com.search.hinaikhan.newyorktimessearch.mvp.NTYResultActivity"
            android:label="@string/title_activity_movie_detail" >
        </activity>

    </application>

</manifest>